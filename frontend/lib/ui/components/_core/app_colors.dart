import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color bgColor = Color(0xFF01103b);
  static const Color amber = Color.from(
    alpha: 1,
    red: 1,
    green: 0.561,
    blue: 0,
  );
}
